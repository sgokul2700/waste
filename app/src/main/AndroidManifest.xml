<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sairamkrishna.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Waste"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainUser" />
        <activity android:name=".MainAdmin" />
        <activity android:name=".MainUserOutput" />
        <activity android:name=".MainAdmin2" />
        <activity android:name=".MainAdmin3" />
        <activity android:name=".MainAdmin1" />
        <activity android:name=".MainAdmin4" />
        <activity android:name=".MainAdmin5" />
        <activity android:name=".MainAdmin6" />
    </application>

</manifest>